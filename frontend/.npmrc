# node_modules is a Windows junction pointing to D:\monitoring-node_modules
# so OneDrive never syncs it. Run setup.ps1 once to create the junction.
#
# This redirects the npm download cache (tarballs) to D:\ as well,
# so re-installs are fast even after deleting the junction target.
cache=C:\dev-modules\.npm-cache
